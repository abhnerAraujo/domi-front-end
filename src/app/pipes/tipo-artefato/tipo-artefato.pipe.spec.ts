import { TipoArtefatoPipe } from './tipo-artefato.pipe';

describe('TipoArtefatoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoArtefatoPipe();
    expect(pipe).toBeTruthy();
  });
});
