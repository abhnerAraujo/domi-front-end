import { TipoResponsavelPipe } from './tipo-responsavel.pipe';

describe('TipoResponsavelPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoResponsavelPipe();
    expect(pipe).toBeTruthy();
  });
});
